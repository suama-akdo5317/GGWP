  <% if user_signed_in? %>
    <%= render 'posts/post', posts: @posts %>

  <% else %>
    <%= render 'layouts/flash_messages' %>
    <div class="jumbotron jumbotron-extend">
      <div class="container-fluid jumbotron-container">
        <div class="row">
          <div class="col-8">
            <h2 class="site-name">Save the NewPlayers.</h2>
            <div class="site-introduction">
              Fender社CEOによると初心者ギタリストの90%が1年以内にギターを辞めています
              <p>初心者ギタリストを助けましょう！</p>
              
            </div>
            <div class="site-introduction2">
              <h2>GGWPは<p>ギタリストのためのコミュニティサイトです</p></h2>
            </div>
          </div>
            <div class="col-4">
              <%= render 'toppages/login' %>
            </div>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</div>